<script>
  import wepy from 'wepy';
  import Tips from '../utils/Tips';
  export default class base extends wepy.mixin {
    loaded() {
      this.init = true;
      this.$apply();
      Tips.loaded();
    }
    methods = {
      nopen() {
        Tips.alert('尚未开放');
      }
    }
  }
</script>
