<template>
  <Loading :init.sync="init"/>

  <view class="column" wx:if="{{init}}">
    <image src="http://img.leshare.shop/shop/other/bargain_bar.jpg" mode="aspectFill" style="width: 750rpx; height:200rpx;"/>

    <repeat for="{{page.list}}" key="index" index="index" item="item">
      <BargainItem :detail.sync="item"/>
    </repeat>

    <!--占位符-->
    <Placeholder :show.sync="isPageEmpty" message="您还没有相关助力商品" />
  </view>

</template>

<script>
  import wepy from 'wepy';
  import base from '../../mixins/base';
  import pagination from '../../mixins/pagination';
  import Loading from '../../components/common/loading';
  import BargainItem from '../../components/bargain/bargain_more_item'
  import assist from '../../api/assist';
  import Placeholder from '../../components/common/placeholder';

  export default class AssistMoreList extends wepy.page {
    data = {
      init: false,
      page: []
    };
    async onLoad () {
      this.page = await assist.assistGoodsList();
      await this.next();
    };
    onShareAppMessage () {
      return {
        title: '上一品品邀请好友助力,免费得'
      };
    }
    methods = {
    };
    computed = {
    };
    components = {
      Loading: Loading,
      BargainItem: BargainItem,
      Placeholder: Placeholder
    };
    mixins = [base, pagination];
    config = {
      navigationBarTitleText: '助力商品',
      enablePullDownRefresh: true
    };
  }
</script>

<style lang="scss">
  @import "../../styles/variable";

</style>
